import 'package:flutter/material.dart';

Bottomsheet(context) {
  return showBottomSheet(
      context: context,
      builder: (context) {
        return Column(
          children: [Text('ok')],
        );
      });
}
