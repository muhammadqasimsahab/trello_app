import 'package:flutter/material.dart';

class highLight_Screen extends StatefulWidget {
  const highLight_Screen({Key? key}) : super(key: key);

  @override
  State<highLight_Screen> createState() => _highLight_ScreenState();
}

class _highLight_ScreenState extends State<highLight_Screen> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("hello i am qasim")),

    );
  }
}
